routes:
  /:
    data: page.home
    template: home
  /about/:
    data: page.about
    template: about
collections:
  /blog/:
    permalink: /blog/{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
