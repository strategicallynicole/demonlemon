routes:
  /:
    data: page.home
    template: home
  /about/:
    data: page.about
    template: about
  /contact/:
    data: page.contact
    template: contact
collections:
  /blog/:
    permalink: /blog/{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
